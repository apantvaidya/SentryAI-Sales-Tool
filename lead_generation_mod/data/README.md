# Data Output Folder

This folder receives JSON outputs produced by the code in `exa_searching/`.

## Expected Outputs

- per-run folders under `data/runs/`
- mapped records from structured entity parsing
- unmapped candidate records
- final accepted lead records in `leads.json`

For the current phase, `public_business_email` should remain `null`.
